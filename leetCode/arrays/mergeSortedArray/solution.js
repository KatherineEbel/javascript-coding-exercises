/*
* Given two integer arrays nums1 and nums2
* sorted in non-decreasing order
* given two integers m and n representing number
* of elements in nums1 and nums2 respectively
* Merge nums1 and nums2 into a single array sorted in
* non-decreasing order
*
* The final sorted array should not be returned by the function,
* but be stored inside the array nums1.
*
* To accommodate this, nums1 has a length of m + n
* where the first m elements denote the elements
* that should be merged, and the last n elements are set to 0
* and should be ignored.
*
* num1 should be the final result
* return undefined
* last n elements in nums1 should be ignored
* iterate over ar2 starting from last element
* store last element of arr1[i]
* loop from end of arr1 while
* */

/**
 * @param {number[]} nums1
 * @param {number} m
 * @param {number[]} nums2
 * @param {number} n
 * @return {void} Do not return anything, modify nums1 in-place instead.
 */
const merge = function (nums1, m, nums2, n) {

};
